1.baseElement文件中MessageBus:
    由原本函数内部this.function的定义方式优化为 prototype的方式
2.baseElement文件中MessageBus.unsubscribe
    增加参数，由原本对应事件名下的所有事件函数全部删除，改为可选删除对应事件名下对应的事件函数,同时修改所有调用的地方(removeEventListener)增加参数接口
3.所有的this.addEventListener
    将原有包装去除，配合removeEventListener的卸载事件功能
4.node文件中baseNode.getTextPostion和baseNode.container
    函数中的getTextPostion增加參數height表明多少行，改了top_center時的文字起始位置
5.DisplayElement和container的setLocation同步属性
6.container.AutoBoundLayout函数重写
7.util文件中优化原有Array.prototype.del函数
8.layout文件中getRootNodes,getRootChilds优化,删除无用util块里的$foreach,$for,mouseCoords,isIntsect,toJson